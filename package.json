{
  "name": "sanover-global-food",
  "version": "1.0.0",
  "description": "Premium B2B Global Food Exporter website",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r assets dist/ && cp *.html *.js *.css dist/ && (cp .env dist/ || true)",
    "start": "echo 'Static site'"
  }
}
